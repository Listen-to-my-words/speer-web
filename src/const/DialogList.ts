import { IDialog } from '../types/IDialogs'
import * as week1 from './week1'

export const weekList1: [Array<IDialog>, Array<IDialog>, Array<IDialog>] = [week1.level1, week1.level2, week1.level2]

export const dialogList: [
  [Array<IDialog>, Array<IDialog>, Array<IDialog>],
  [Array<IDialog>, Array<IDialog>, Array<IDialog>],
  [Array<IDialog>, Array<IDialog>, Array<IDialog>],
  [Array<IDialog>, Array<IDialog>, Array<IDialog>]
] = [weekList1, weekList1, weekList1, weekList1]
