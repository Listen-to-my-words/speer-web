import { Box } from '@mui/material'
import React from 'react'

const Logo = () => {
  return <Box component={'img'} src={'https://picsum.photos/90'} width={'8rem'} height={'8rem'} />
}

export default Logo
